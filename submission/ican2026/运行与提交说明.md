# VisionGuard AI - iCAN 2026运行与提交说明

## 提交身份

- 赛事：2026年iCAN大学生创新创业大赛AI应用创新挑战赛
- 赛道：软件赛道
- 组别：高校组
- 项目：VisionGuard AI - 面向工业安全场景的多模态视觉风险智能体
- GitHub：https://github.com/shuabirfana-a11y/VisionGuard-AI
- 源码基线：`chore/initial-stage4-baseline`，提交 `a14baf1`
- 代码审阅：https://github.com/shuabirfana-a11y/VisionGuard-AI/pull/5（GitHub自动测试已通过）

## 源码包内容

源码ZIP由Git提交基线直接导出，包含应用代码、网页界面、自动化测试、评测脚本、知识库、模型卡和运行说明；不包含虚拟环境、上传图片、密钥及大体积模型权重。

## 最小运行方式

1. 安装Python 3.11。
2. 在项目根目录创建虚拟环境并安装开发依赖：`pip install -e ".[dev]"`。
3. 启动服务：`uvicorn app.main:app`。
4. 浏览器访问：`http://127.0.0.1:8000`。

未配置专业模型时，系统会显式启用Demo检测器，基础闭环仍可运行，但页面会记录fallback状态，不能把该结果作为专业模型效果证明。

## 比赛完整运行方式

Windows比赛机在项目根目录执行：

`powershell -ExecutionPolicy Bypass -File scripts\start_competition.ps1`

脚本会检查YOLO权重及摘要，启用本地火情分类环境和本地大模型（条件具备时），并启动网页。服务启动后先点击“运行赛前环境自检”，确认专业视觉模型、火情复核模型、本地大模型、安全知识库、Agent工具、验证案例和报告导出均为就绪状态。

## 验证结果

- 自动化测试：37项通过。
- 公开演示档位：已完成Dockerfile、Render Blueprint、运行档位标识、上传限制、并发保护和安全响应头；本机缺少Docker，尚未执行镜像构建验证。
- 报告与证据导出：可打印HTML、结构化JSON与浏览器端标注证据PNG。
- 源码不依赖FactorySafe；FactorySafe数字孪生、路径规划、人员核查与事件管理不属于本项目。

## 提交提醒

- 应用方案PDF已控制在15页，符合不超过20页要求。
- 项目Logo已提供为720×720 PNG，可直接用于报名系统上传。
- 演示视频按官方要求需为MP4且不超过5分钟，本次未制作。
- 如报名系统要求在线链接，可填写GitHub仓库地址；若要求可直接访问的网页，可按`docs/PUBLIC_DEPLOYMENT.md`在Render创建Blueprint。正式提交前仍须由团队账号完成部署、预热和公网访问复核。
